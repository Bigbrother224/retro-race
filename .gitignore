# Build artifacts
.build/
*.dylib
*.o
*.so
cores/*/src/board*.o

# ROMs (user-owned content, never committed)
app/Roms/
*.nes
*.bin

# Temp
.DS_Store
cores/libretro-fceumm/
cores/libretro-snes9x/

# Boxart images (user-owned content, optional)
app/Boxart/

# Replay exports (user-generated, optional)
app/Replays/
